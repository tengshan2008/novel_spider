from .crawel import Page, Pagination

# def check_novel(url, start_page, end_page):
#     pagination = Pagination(url, page_type='out')
#     for i , link in pagination.links[start_page-1:end_page]:
#         page = Page(link, i)
#         for item in page.get_items()
